interface:
  display_name: "Pinpoint"
  short_description: "Repo-specific patterns and workflows for pinpoint"
  default_prompt: "Use the pinpoint repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true